version: "3.9"
services:
  email-assistant:
    build: .
    container_name: email_assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app/static:/app/app/static
    restart: unless-stopped
